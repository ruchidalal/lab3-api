# Repository Copilot Instructions

- Keep edits small and targeted to the request.
- Preserve existing API behavior unless the request says to change it.
- Add or update tests for behavior changes in `src/routes` and `src/middleware`.
- Run the test suite after code changes when possible.
- Prefer TypeScript strict typing and avoid `any` unless unavoidable.
- Keep HTTP status codes consistent with existing route patterns.
